VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Gemini API key — local dev only (optional if using Vercel proxy)
VITE_GEMINI_API_KEY=your-gemini-api-key-here

# Server-side Gemini API key — set in Vercel dashboard, NOT in .env
# GEMINI_API_KEY=your-gemini-api-key-here

# Vapi voice chat — public key (client-side, safe to expose)
# Ottieni la tua key su https://dashboard.vapi.ai → Keys
VITE_VAPI_PUBLIC_KEY=your-vapi-public-key-here
